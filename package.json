{
  "name": "sigma-calendar-heatmap-plugin",
  "version": "1.0.0",
  "description": "A calendar heatmap plugin for Sigma Computing — shows aggregated daily metrics with intensity-based coloring",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sigmacomputing/plugin": "^1.0.9",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0"
  }
}
